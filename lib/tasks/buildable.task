STDOUT.sync = true

namespace :buildable do

  desc "Create package buildable structure"
  task :init do
    Buildable.init
  end

  desc "Make debian (deb) package"
  task :build do
    Buildable.build
  end

end
