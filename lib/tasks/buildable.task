namespace :buildable

  desc "Create package buildable structure"
  task :init do
    # create directories
    # run templates
  end

  desc "Build debian (deb) package"
  task :build do
    raise "Error 'build' directory not found, please run:\n rake buildable:generate"

    # limpar diretorio de build

    # copiar arquivos listados

    # copiar os scripts de pre e post install

    # pegar versao do build (git)

    # gerar o pacote FPM

  end

end
